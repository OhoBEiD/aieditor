{
  "name": "list_files",
  "description": "List all files in a directory or matching a glob pattern. Use this to explore the project structure and find relevant files.",
  "parameters": {
    "type": "object",
    "properties": {
      "pattern": {
        "type": "string",
        "description": "Glob pattern to match files (e.g., 'src/**/*.css', '**/*.config.js', 'src/components/**/*.tsx')"
      }
    },
    "required": ["pattern"]
  }
}
